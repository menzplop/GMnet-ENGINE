/// show_priority_destroy(priority index, script, line);
if !is_undefined(argument0)
{
    show_debug_message("Priority: " + string(argument0) + " destroyed in " + string(argument1) + " line " + string(argument2));
}
